{
  "name": "cfn-reducer",
  "version": "0.0.0",
  "description": "CloudFormation template reducer",
  "keywords": [
    "cloudformation"
  ],
  "license": "MIT",
  "scripts": {
    "lint": "eslint ./src/**/*.js ./test/**/*.js",
    "test": "npm run lint && node ./test/index.js | tap-dot",
    "cov": "npm run lint && istanbul cover --dir ./test/coverage ./test/index.js | tap-spec"
  },
  "dependencies": {
    "eslint": "3.0.1",
    "istanbul": "0.4.4",
    "require-dir": "0.3.0",
    "rfr": "1.2.3",
    "tap-dot": "1.0.5",
    "tap-spec": "4.1.1",
    "tape": "4.6.0"
  }
}
